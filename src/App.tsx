function App() {
    // Temporary placeholder for the 0.2.0.0 bootstrap. Real UI and i18n arrive
    // in the 0.3.0.0 foundation (MUI, TanStack Query, react-i18next, router).
    return (
        <main>
            <h1>news-feed-client</h1>
            <p>Vite + React + TypeScript is up and running.</p>
        </main>
    )
}

export default App
